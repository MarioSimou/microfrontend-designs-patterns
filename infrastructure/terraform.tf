# create a vpc
# create a load balancer with the associated security groups
# create targets that point to the ECS services

# create registries for shell, auth and posts
# create an ECS cluster
# create an ECS services for shell, auth and posts
# create task definition of shell, auth and posts